
IF !DEF(HEAP_INC) ;include guard
    DEF HEAP_INC EQU 1

DEF WRAM_SIZE               EQU $1000
DEF NUM_BANK                EQU $7

DEF HEAP_HEADER_0_SIZE      EQU $1 * $10
DEF HEAP_HEADER_1_SIZE      EQU $6 * $10
DEF HEAP_HEADER_2_SIZE      EQU $6 * $10
DEF HEAP_HEADER_SIZE        EQU HEAP_HEADER_0_SIZE + HEAP_HEADER_1_SIZE + HEAP_HEADER_2_SIZE

DEF HEAP_HEADER_1_ENTRY_SIZE EQU $2
DEF HEAP_HEADER_1_ENTRIES    EQU HEAP_HEADER_1_SIZE / HEAP_HEADER_1_ENTRY_SIZE
DEF HEAP_HEADER_2_ENTRY_SIZE EQU $4
DEF HEAP_HEADER_2_ENTRIES    EQU HEAP_HEADER_2_SIZE / HEAP_HEADER_2_ENTRY_SIZE

DEF HEAP_HEADER_0           EQU $D000
DEF HEAP_HEADER_1           EQU HEAP_HEADER_0 + HEAP_HEADER_0_SIZE
DEF HEAP_HEADER_2           EQU HEAP_HEADER_1 + HEAP_HEADER_1_SIZE
DEF HEAP_START              EQU HEAP_HEADER_2 + HEAP_HEADER_2_SIZE

DEF HEAP_END                EQU HEAP_HEADER_0 + WRAM_SIZE - 1

DEF HEAP_HEADER_0_NUMADDRESS           EQU HEAP_HEADER_0
DEF HEAP_HEADER_0_NUMGAPS              EQU HEAP_HEADER_0_NUMADDRESS + $1
DEF HEAP_HEADER_0_ENDOFMEM             EQU HEAP_HEADER_0_NUMGAPS + $1
DEF HEAP_HEADER_0_LASTALLOCS           EQU HEAP_HEADER_0_ENDOFMEM + $2
;+2



    ENDC ;HEAP_INC